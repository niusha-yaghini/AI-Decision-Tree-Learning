import Attribute
import Program as p
from enum import Enum


# # enum att:
# class att(Enum):
#     age = 1
#     ticket = 2
#     cabin = 3
    


class Example:
    def __init__(self, example):
        self.pclass = example[0]
        # # self.properties = p.AttributesNames
        # # self.example = list(example)
        # # for pr in p.AttributesNames:
        # i = 1
        # for a in att:
        #     self.i = example[i-1]
        #     i+=1
            
            
# p1 = Example([5, 4, 9])

# print (p1)